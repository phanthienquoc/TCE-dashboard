{
  "name": "web",
  "projectType": "application",
  "sourceRoot": ".",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "vite build" }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": { "command": "vite" }
    }
  }
}
