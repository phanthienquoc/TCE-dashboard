{
  "name": "web",
  "projectType": "application",
  "sourceRoot": "apps/web",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "next build apps/web" }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": { "command": "next dev apps/web" }
    }
  }
}
