'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
import { useAuthStore } from '../../lib/store';
export default function LoginPage(){const router=useRouter();const login=useAuthStore(s=>s.login);const mfa=useAuthStore(s=>s.mfa);const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [code,setCode]=useState('');const [pending,setPending]=useState<string|null>(null);const [error,setError]=useState('');const [busy,setBusy]=useState(false);
 const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{if(pending){await mfa(pending,code);}else{const r=await login(email,password);if(r.mfaRequired){setPending(r.userId!);setBusy(false);return;}}router.replace('/');}catch(err:any){setError(err?.response?.data?.message??'Unable to sign in');}finally{setBusy(false)}};
 return <main className="shell"><div className="card" style={{maxWidth:420,margin:'12vh auto'}}><h1>TCE Dashboard</h1><p className="muted">Sign in to continue.</p><form onSubmit={submit}>{pending?<div className="field"><label>MFA code</label><input autoFocus value={code} onChange={e=>setCode(e.target.value)} inputMode="numeric" /></div>:<><div className="field"><label>Email</label><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} /></div><div className="field"><label>Password</label><input type="password" required value={password} onChange={e=>setPassword(e.target.value)} /></div></>}{error&&<p className="error">{error}</p>}<div className="actions"><button disabled={busy}>{busy?'Working…':pending?'Verify':'Sign in'}</button></div></form></div></main>}
