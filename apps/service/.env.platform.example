# 32-byte AES-256-GCM key encoded as 64 hex characters.
# Generate with: openssl rand -hex 32
TCE_CREDENTIAL_ENCRYPTION_KEY=

# Existing service secrets
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
JWT_SECRET=
