{
  "name": "service",
  "projectType": "application",
  "sourceRoot": "apps/service/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "npm run build", "cwd": "apps/service" }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": { "command": "npm run dev", "cwd": "apps/service" }
    }
  }
}
