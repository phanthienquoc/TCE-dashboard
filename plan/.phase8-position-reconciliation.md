# Phase 8 — Position Reconciliation

Status: VERIFICATION PENDING
Issue: #496
Phase branch: `phase/tce-auto-trade-phase-8-position-reconciliation`

## Objective

Establish authoritative, restart-safe position truth for downstream dividend lifecycle and automatic take-profit execution.

## Completed implementation

- Normalize provider holdings into provider-neutral TCE position state.
- Reconcile local positions with provider truth deterministically.
- Project only unambiguous provider truth into authoritative `HOLDING` positions for downstream lifecycle/exit logic.
- Track quantity, average/entry price, current price, target price, sellability and lifecycle status in the provider-neutral position contract.
- Rebuild slot/capital ownership deterministically from authoritative position state after restart.
- Detect missing, unexpected and ambiguous positions; fail closed where truth is unclear.
- Keep reconciliation provider-neutral and prevent live order submission from reconciliation itself.
- Add deterministic tests for converged, missing, unexpected, ambiguous, restart-recovery and authoritative projection states.
- Align Phase 8 reconciliation specs with the canonical native `node:test` backend test runner so the deterministic suites do not depend on an undeclared Vitest runtime.
- Make the CI Vitest invocation self-contained so `npx vitest@5` does not auto-load the repository config that imports the undeclared local `vitest/config` package.
- Add CI-only Jest-global compatibility for legacy Vitest specs without changing production or live-order behavior.

## Tasks

- [x] #498 Provider-neutral position reconciliation core
- [x] #500 Restart-safe position persistence and ownership rebuild
- [x] #502 Enable runtime verification on Phase branch
- [x] #504 Authoritative reconciled HOLDING projection
- [x] #508 Align reconciliation specs with native Node test runner
- [x] #512 Fix native test-runner/reconciliation verification failures
- [x] #525 Fix CI Vitest config resolution
- [x] #530 Fix CI Vitest legacy Jest-global compatibility
- [ ] #514 Runtime/CI verification rerun and final Phase 8 sign-off
- [x] #536 Verification trigger checkpoint after #530
- [x] #538 Mark verification checkpoint task complete

## Verification note

Workflow #1008 completed with 121 native tests passing, then failed in the Vitest stage because `vitest.config.ts` imports `vitest/config` while the repository does not declare Vitest locally. Task #525 added a self-contained CI config; Task #530 added CI-only Jest-global compatibility for legacy Vitest specs. The Phase 8 backend workflow is present on `phase/**` and runs `npm install --include=dev`, `npm test`, and `npm run build:service`. The Phase 8 head after verification checkpoint #536 is `507865abb70a6a52cad222bfe254f1d5f37bbce6`; the GitHub API currently reports no workflow run attached to that head, so final runtime/CI sign-off remains pending. CI is informational/non-blocking and does not block dependency-safe implementation.

## Dependency

Phase 8 must be verified complete before Phase 9 and before Phase 10 automatic +5% exit can safely create EXIT_READY/SELL intents from authoritative HOLDING positions.