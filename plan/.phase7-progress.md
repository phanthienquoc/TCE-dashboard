# Phase 7 — Order Reconciliation

Status: IN PROGRESS

## Objective
Make local engine order state converge with broker truth without leaking provider-specific SDK/HTTP details into TCE contracts.

## Task #459

- [x] Define provider-neutral reconciliation query/result contract.
- [x] Define canonical provider-status → `TceOrderState` mapping.
- [x] Fail closed on unknown provider statuses by mapping them to `UNKNOWN`.
- [x] Add deterministic mapper tests.
- [x] Export reconciliation contract and mapper through the public TCE surfaces.

## Task #462

- [x] Implement concrete SSI order-reconciliation adapter.
- [x] Validate TCE account/environment before provider reads.
- [x] Normalize SSI order states through the provider-neutral mapper.
- [x] Fail closed on unknown provider statuses.
- [x] Add deterministic adapter tests.

## Task #464

- [x] Implement local-order vs SSI-state reconciliation core.
- [x] Match local/provider orders by provider order id with client request id fallback.
- [x] Detect missing provider orders.
- [x] Detect unknown/orphan provider orders.
- [x] Handle partial fills and recognized terminal states deterministically.
- [x] Fail closed on unknown provider state without mutating local state.
- [x] Add deterministic reconciliation tests.

## Task #466

- [x] Define provider-neutral reconciliation persistence contract.
- [x] Persist reconciliation results under a stable run identifier.
- [x] Preserve account/environment and recorded timestamp metadata.
- [x] Make repeated writes for the same run idempotent.
- [x] Add deterministic in-memory persistence tests.

## Task #470

- [x] Define provider-neutral reconciliation lifecycle event types.
- [x] Map every reconciliation disposition to an explicit lifecycle event.
- [x] Preserve run/account/environment/correlation identity and reconciliation metadata.
- [x] Derive deterministic event identity for idempotent downstream handling.
- [x] Add deterministic lifecycle-event tests.

## Task #479

- [x] Define provider-neutral lifecycle sink lookup/publish boundary.
- [x] Replay persisted reconciliation runs into lifecycle events.
- [x] Validate persisted run/account/environment identity before replay.
- [x] Publish only unseen deterministic event identities.
- [x] Resume safely after partial sink progress without duplicates.
- [x] Fail closed when the persisted run is missing or mismatched.
- [x] Add deterministic recovery/idempotency tests.
- [ ] Wire lifecycle events into a concrete transport/persistence sink.
- [ ] Complete final Phase 7 verification.

## Dependency frontier

Next dependency-safe task: concrete lifecycle-event transport/persistence sink, followed by final Phase 7 verification and promotion.

## Safety rules

- Unknown execution outcomes require reconciliation before resubmission.
- Reconciliation must not mutate an order into a terminal state from an unrecognized provider status.
- Account/environment mismatches fail closed.
- Provider credentials and SDK DTOs remain inside the adapter.
- Orphan provider orders are reported only; they are never auto-adopted or resubmitted.
- Missing provider orders are reported only; automatic resubmission remains outside reconciliation.
- Lifecycle events are descriptive and do not authorize adoption, resubmission, or provider mutation.
- Recovery replay is idempotent by deterministic event identity and never authorizes provider mutation.
- CI remains informational/non-blocking; market holidays/closed sessions do not stop implementation progression.
