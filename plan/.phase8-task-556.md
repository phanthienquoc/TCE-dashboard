# Task #556 — Unblock Phase 8 backend verification after Vitest runtime failures

Status: IMPLEMENTED
Issue: #556
Phase: Phase 8 — Position Reconciliation
Phase branch: `phase/tce-auto-trade-phase-8-position-reconciliation`

## Objective

Resolve the deterministic Vitest failures exposed by Phase 8 workflow #1075 so the canonical backend verification command can complete without weakening production or live-order behavior.

## Changes

- Add the `@tce/ssi` workspace alias to the CI-only Vitest config used by `npx vitest@5`.
- Configure positive HuntingDividendDecisionEngine fixtures with an explicit low confidence threshold matching the current score normalization, while retaining the default rejection test for zero-confidence candidates.
- Replace the assisted-approval regex over the entire serialized request with explicit assertions that sensitive credential/OTP fields are absent; the approval action name is intentionally `APPROVE_OR_ENTER_OTP` and is not itself a credential value.

## Verification

Run the Phase 8 backend workflow after merging this single clean task commit. No live order submission or SSI mutation is permitted.
