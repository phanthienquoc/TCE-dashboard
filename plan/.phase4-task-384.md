# Task 384 — Order Planner foundation

Phase 4 task: convert a provider-neutral TCE decision into an executable provider-neutral order plan and execution intent.

Acceptance:

- deterministic board-lot quantity calculation;
- no order plan when capital, buying power, slot, price, or constraints are invalid;
- target and invalidation carried through;
- deterministic order-plan identity;
- deterministic execution-intent idempotency key;
- no SSI/provider/DB coupling;
- focused deterministic tests;
- task commits will be squashed to one clean commit before merge into Phase 4.
