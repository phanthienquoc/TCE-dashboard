# Phase 6 — Execution Engine + SSI Adapter

Status: IN PROGRESS

## Implemented

- [x] Define provider-neutral execution commands/results.
- [x] Define submit/cancel/replace command semantics.
- [x] Define execution error taxonomy with retry/reconciliation semantics.
- [x] Define provider order ID and client request ID mapping.
- [x] Define explicit Risk/Safety-Gate approval reference on execution commands.
- [x] Preserve PAPER / ASSISTED / LIVE modes at the execution contract boundary.
- [x] Keep SSI SDK/HTTP details outside the TCE domain contract.
- [x] Implement provider-neutral execution orchestrator over approved execution envelopes.
- [x] Add provider-neutral trading authorization lifecycle boundary before non-PAPER execution.
- [x] Implement concrete SSI trading authorization adapter over the existing credential/token lifecycle.
- [x] Register the SSI authorization adapter in the service platform module.
- [x] Verify valid persisted SSI authorization, account/environment identity, and fail-closed credential/account failures.
- [x] Implement deterministic PAPER execution simulation.
- [x] Handle expired trading authorization before submission through the SSI adapter with refresh/reauth coverage.
- [x] Add provider-neutral OTP / approval challenge boundary without exposing SSI credentials or OTP values.

## Remaining

- [ ] Implement ASSISTED user-approval handoff.
- [ ] Implement LIVE SSI submission.
- [ ] Verify account/environment selection and fail-closed behavior end-to-end.
- [ ] Complete Phase 6 verification and merge Phase 6 to master.

## Task #426

- GitHub issue #426 / PR #427 — provider-neutral execution contract.
- Branch: `task/426-phase-6-execution-contract` from Phase 6.
- Added `TceExecutionCommand` SUBMIT/CANCEL/REPLACE variants and normalized `TceExecutionResult` / error taxonomy.
- Execution commands carry explicit Risk/Safety-Gate approval identity, correlation ID and idempotency key.
- Added deterministic contract coverage for submit/cancel/replace shapes.
- Task branch was squashed to exactly one clean commit `1016d8b072a9e63ee41de66f3bcf561c2cfefbb9` before PR #427 and merged into Phase 6 as `0d73593fddafd10af86ca1ac980ce53ecfc3da52`.
- CI remains informational and does not block dependency-safe progression.

## Task #428

- GitHub issue #428 / PR #429 — provider-neutral execution orchestrator.
- Branch: `task/428-phase-6-execution-orchestrator` from Phase 6.
- Dispatches approved SUBMIT/CANCEL/REPLACE commands only through `TceExecutionPort`.
- Enforces approval-to-intent correlation/idempotency/mode identity and fails closed on incomplete commands.
- Deduplicates completed requests by idempotency key and converts adapter exceptions to `UNKNOWN` with reconciliation required.
- Added deterministic orchestrator tests for dispatch, deduplication, authorization mismatch and unknown adapter failure.
- Task branch was squashed to exactly one clean commit `e1c694c7b1e2df814c20bee493b8666375da1915` before PR #429 and merged into Phase 6 as `e46f2a802aecae52f4ce180415ef0f9a4290af7b`.
- CI remains informational and does not block progression.

## Task #430

- GitHub issue #430 / PR #430 — provider-neutral trading authorization lifecycle boundary.
- Branch: `task/430-phase-6-trading-authorization-boundary` from Phase 6.
- Added `TceTradingAuthorizationPort` and authorization states `READY`, `EXPIRED`, `APPROVAL_REQUIRED`, `UNAVAILABLE`.
- Added `TceTradingAuthorizedExecutionPort`: PAPER bypasses provider authorization; ASSISTED/LIVE must have a READY trading session before provider execution; expired/approval-required/unavailable states fail closed.
- Added deterministic tests for PAPER bypass, approval-required, expired authorization and READY LIVE execution.
- Task branch was squashed to exactly one clean commit `d22accbc912c323be5fdc2bbdd3a9a7ba9628b94` before PR #430 and merged into Phase 6 as `2fa526ed1e4864d5bd52f92303dd2d41843ecaea`.
- CI remains informational and does not block progression.

## Task #431

- GitHub issue #431 — concrete SSI trading authorization adapter.
- Branch: `task/431-phase-6-ssi-authorization-adapter` from Phase 6.
- Added `TceSsiTradingAuthorizationAdapter` over `PlatformCredentialPort` and the existing `SsiBrokerAdapter` token lifecycle.
- Resolves the TCE account to its owning user and selected SSI account, preserving account/environment identity.
- Reuses a valid access token without network activity; otherwise invokes the existing SSI refresh/reauth lifecycle and maps reauthentication to `APPROVAL_REQUIRED`.
- Persists refreshed token snapshots through the existing encrypted credential store.
- Registered the adapter in `PlatformCredentialsModule` for the next execution-boundary wiring task.
- Added deterministic tests for READY persisted authorization, account mismatch, missing account and credential failure.
- Task branch was squashed to exactly one clean commit `f9c2c156861b0edeb5e2c892021b3f4ee7d3adb9` before PR #432 and merged into Phase 6 as `86e125e52261a08182b164919dd036e83d36bfac`.
- CI remains informational and does not block progression.

## Task #433

- GitHub issue #435 / PR #436 — PAPER execution simulator.
- Branch: `task/433-phase-6-paper-execution-simulator` from Phase 6.
- Added `TcePaperExecutionPort` with deterministic in-memory SUBMIT/CANCEL/REPLACE semantics and synthetic provider order IDs.
- PAPER submission is provider-free, idempotent by execution authorization identity, accepts zero-fill simulation, and fails closed on invalid order data.
- Cancel/replace require matching execution-intent/provider-order identity and reject invalid or cancelled references.
- Task branch was squashed to exactly one clean commit `04e2b8c9b08d8ed937eacfdbb463950fb7e6846b` before PR #436 and merged into Phase 6 as `6da6992f023776de99d91b0eccd1320bbf9c7786`.
- CI remains informational and does not block progression.

## Task #441

- GitHub issue #441 — SSI expired authorization and reauth mapping.
- Branch: `task/441-phase-6-ssi-expired-authorization` from Phase 6.
- Fixed the adapter to inspect the `SsiBrokerAdapter.connect()` `ContractResult` instead of treating a failed connection as an exception.
- Reauthentication-required failures now deterministically surface `APPROVAL_REQUIRED`; provider token-expired failures surface `EXPIRED`; unknown failures remain fail-closed as `UNAVAILABLE`.
- Added deterministic mapping tests for reauth-required, expired-token, and unknown authorization failures while retaining persisted READY/account safety coverage.
- Task branch was squashed to exactly one clean commit before PR merge.
- CI remains informational and does not block dependency-safe progression.

## Task #443

- GitHub issue #443 — SSI OTP / approval boundary.
- Branch: `task/443-phase-6-ssi-otp-approval-boundary` from Phase 6.
- Extended the provider-neutral trading authorization contract with `requestApproval()` and explicit approval metadata: transaction reference, action, and user-facing message.
- Added SSI adapter approval-challenge mapping through the existing SSI `requestOtp()` boundary; OTP/credentials remain inside the provider adapter and are never returned in TCE authorization data.
- Preserved account/environment identity and fail-closed credential/account validation before requesting an approval challenge.
- Added deterministic pure mapping coverage for approval metadata and credential non-disclosure.
- Task branch was created from the Phase 6 head and contains one clean task commit before merge.
- CI remains informational and does not block dependency-safe progression.
