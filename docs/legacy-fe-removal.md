# Legacy FE removal

The repository now uses the Next.js application under `apps/web` as the only frontend. The previous Vite-style frontend under `src/` is removed so it cannot be copied into the frontend image or accidentally become a second source of browser code.
