Pool 5 is research state; buy_candidates is execution intent. Promotion requires a TRIGGERED pool row, an available position slot, and a positive score. With max_positions=2, no new execution candidates should be promoted while both positions are occupied.